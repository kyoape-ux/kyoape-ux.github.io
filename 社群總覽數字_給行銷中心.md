# 影音平台「社群總覽」數字 — 給行銷整合分析中心抓取用

> 影片發布平台（ktgh-video-manager.pages.dev，v2.88）已新增「社群總覽」頁，把影音內容在各平台的**數量**（非比率）算好給長官看。
> 行銷中心要把這些數字同步到它的「社群平台總覽」的**影音(FB/IG/YT)**欄位。這份說明數字定義、計算法、與串接建議。

## 一、要抓的數字（每個平台：則數 / 觀看數 / 互動數 / 觸及數）

| 平台 | 則數 | 觀看數 | 互動數 | 觸及數 |
|---|---|---|---|---|
| YouTube | 有 YT 連結或觀看的影片數 | Σ yt_views | Σ(yt_likes+yt_comments) | Σ(yt_imp，無則 yt_views) |
| Facebook | 有 FB 連結或觀看的影片數 | Σ fb_views | Σ(fb_likes+fb_comments+fb_shares) | Σ fb_reach |
| Instagram | 有 IG 的影片 + IG 原生貼文數 | Σ ig_views + Σ IG貼文 views | Σ(ig_likes+comments+shares) + Σ IG貼文(讚+留言+分享+收藏) | Σ ig_reach + Σ IG貼文 reach |

- 觀看＝各平台影片觀看；互動＝讚＋留言＋分享（IG 另計收藏）；觸及＝FB 觸及人數／IG 觀看／YT 曝光（無曝光以觀看計）。
- **已對應到影片的 IG 貼文不重複計算**（用 `igStandalone()` 只取未綁定影片的原生貼文）。
- 可帶期間：全部 / 某年 / 某年某月。

## 二、計算法（影音平台的實作，可照抄）

資料源：`活動影片 activeVideos()`（未刪除的影片記錄，含 yt_/fb_/ig_ 欄位）＋ `igStandalone()`（未對應影片的 IG 原生貼文）。

```js
function socialSummary(prefix){ // prefix：''＝全部、'2026'＝該年、'2026-09'＝該月
  const N=(a,k)=>a.reduce((s,d)=>s+(Number(d[k])||0),0);
  const inP=d=>!prefix||String(d.date||'').startsWith(prefix);
  const vids=activeVideos().filter(inP);
  const igAll=igStandalone().filter(inP);
  const yt={count:vids.filter(d=>d.yt_url||Number(d.yt_views)).length, views:N(vids,'yt_views'),
    eng:vids.reduce((s,d)=>s+(+d.yt_likes||0)+(+d.yt_comments||0),0),
    reach:vids.reduce((s,d)=>s+((+d.yt_imp||0)||(+d.yt_views||0)),0)};
  const fb={count:vids.filter(d=>d.fb_url||Number(d.fb_views)).length, views:N(vids,'fb_views'),
    eng:vids.reduce((s,d)=>s+(+d.fb_likes||0)+(+d.fb_comments||0)+(+d.fb_shares||0),0),
    reach:N(vids,'fb_reach')};
  const ig={count:vids.filter(d=>d.ig_url||Number(d.ig_views)).length+igAll.length,
    views:N(vids,'ig_views')+N(igAll,'views'),
    eng:vids.reduce((s,d)=>s+(+d.ig_likes||0)+(+d.ig_comments||0)+(+d.ig_shares||0),0)
        +igAll.reduce((s,d)=>s+(+d.likes||0)+(+d.comments||0)+(+d.shares||0)+(+d.saves||0),0),
    reach:N(vids,'ig_reach')+N(igAll,'reach')};
  const total={count:yt.count+fb.count+ig.count,views:yt.views+fb.views+ig.views,eng:yt.eng+fb.eng+ig.eng,reach:yt.reach+fb.reach+ig.reach};
  return {period:prefix||'全部',yt,fb,ig,total};
}
```

回傳 JSON 範例：
```json
{ "period":"2026-09",
  "yt":{"count":1,"views":1000,"eng":55,"reach":1000},
  "fb":{"count":2,"views":14000,"eng":155,"reach":11700},
  "ig":{"count":1,"views":500,"eng":20,"reach":600},
  "total":{"count":4,"views":15500,"eng":230,"reach":13300} }
```

## 三、串接方式（三選一，建議 B）

影片平台資料存在它自己的 Google Sheet（GAS 後端「光田影片管理系統 後端」，部署 `AKfycbxDla5S…`，走 Cloudflare Pages `/api` 代理、Access 保護、指令碼屬性 `API_KEY` 鎖）。

- **A. 直接用現有端點自己加總**：影片平台已有 `GET /api?module=youtube&action=getVideos`（回全部影片）與 `action=getIgPosts`（回 IG 貼文）。行銷中心後端帶影片平台的 `GAS_URL`+`key` 呼叫，取回後套上面 `socialSummary()` 加總。缺點：要把影片平台的 GAS 金鑰放進行銷中心。
- **B（建議）. 在影片平台 GAS 加一個彙總端點 `getSocialSummary`**：回傳上面那包 JSON（可帶 `ym` 參數）。行銷中心只要打一個輕量端點就好，不用搬全部資料、也不用懂加總邏輯。
  - GAS 端：`doGet` 加 `if(action==='getSocialSummary') return jsonRes(getSocialSummary(e.parameter.ym));`，`getSocialSummary(ym)` 讀 yt_videos+ig_posts 兩張表用同一套定義加總。
  - ⚠️ 注意：影片平台線上 GAS `Code.gs` 與本機 `files/apps-script-youtube-module.js` 已不同步（有線上才有的 dur 修正），**改 GAS 要以線上為準、勿用本機檔覆蓋**；部署要「管理部署→編輯→版本選『建立新版本』（用 find 取 ref 點，勿用座標）」。
- **C. 共用一張 Google Sheet**：影片平台每次算完把 summary 寫進一張兩邊都讀得到的 Sheet，行銷中心讀那張。適合排程、不即時。

## 四、對應到行銷中心「社群平台總覽」

行銷中心那張表的定義：追蹤數 / 互動數(FB=讚+點擊、IG=讚+留言+分享+收藏、YT=讚+留言+分享) / 觸及數(FB=觸及、IG=觀看、YT=曝光)。
影音平台提供的是**影音(FB/IG)＋YouTube**這部分的 觀看/互動/觸及 數字，填進該表的「影音(FB)」「Instagram」「YouTube 頻道」對應欄位即可（追蹤數仍由行銷中心自己維護，影片平台沒有追蹤數）。互動定義略有差異（影片平台 FB 互動用 讚+留言+分享，沒有點擊數），對齊時以行銷中心為準、或雙方統一。

---

## 五、行銷中心已完成串接（2026-09-22）

- 採方案 B。影片平台 GAS「光田影片管理系統 後端」部署 `AKfycbxDla5S…` **第 19 版**新增 `getSocialSummary`：
  - 參數：`from`、`to`（YYYY-MM-DD，含頭尾）或 `ym`（`2026`／`2026-09`）；都不帶＝全部。
  - 回傳：`yt`／`fb`／`ig`／`total`（各含 count, views, eng, reach, likes, comments, shares, saves）＋ `months`（逐月同結構）＋ `igOwnedExcluded`（排除的已對應 IG 貼文數）＋ `generatedAt`。
  - 定義與前端 `socialSummary()`／`igOwnerMap()` 完全相同。
- 同版本另有第 18 版加的 `ytChannel`（頻道訂閱數，唯讀）。
- 行銷中心經自己的 `/api/video` 唯讀代理呼叫（Access 驗證＋金鑰只在伺服器），不持有影片平台金鑰。
- 對應方式：YouTube、Instagram 的互動與觸及用這包數字；FB 仍以粉專貼文匯出檔為準（有點擊數），**某月粉專貼文尚未匯入時，FB 影音暫以本端點的 fb 數字補位**。
- 改影片平台 GAS 時請保留 `getSocialSummary`、`ytChannel` 兩個 action 與 `fmtCell(v, h)` 的時長處理（線上 Code.gs 為準）。
